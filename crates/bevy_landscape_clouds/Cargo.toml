[package]
name = "bevy_landscape_clouds"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.18" }

[target.'cfg(unix)'.dependencies]
bevy = { version = "0.18", features = ["dynamic_linking"] }
